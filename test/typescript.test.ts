/* eslint-disable */

import * as ts from "typescript";
describe('typescript', () => {
    test('typescript test', async (done) => {
        console.log('------')
        done()
    })

    test('typescript test2', async (done) => {
        done()
    })

})
